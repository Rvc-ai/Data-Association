<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen">

    <div class="w-full max-w-md p-8 bg-zinc-800 rounded-lg shadow-lg">
        <h3 class="text-3xl font-semibold text-center mb-6">Create Account</h3>

        <form action="/register" method="post" class="flex flex-col gap-4">
            <input class="px-4 py-3 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:border-blue-500 transition" 
                   type="text" placeholder="Name" name="name" required>

            <input class="px-4 py-3 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:border-blue-500 transition" 
                   type="text" placeholder="Username" name="username" required>

            <input class="px-4 py-3 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:border-blue-500 transition" 
                   type="password" placeholder="Password" name="password" required>

            <input class="px-4 py-3 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:border-blue-500 transition" 
                   type="email" placeholder="Email" name="email" required>

            <input class="px-4 py-3 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:border-blue-500 transition" 
                   type="number" placeholder="Age" name="age" required>

            <button class="mt-2 bg-blue-500 hover:bg-blue-600 transition text-white font-medium py-3 rounded-md">
                Create Account
            </button>
        </form>
    </div>

</body>
</html>
