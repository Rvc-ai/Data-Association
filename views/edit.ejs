<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white flex justify-center items-center min-h-screen p-5">
    
    <div class="w-full max-w-lg bg-zinc-800 p-8 rounded-lg shadow-lg">
      <!-- Logout Button -->
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 hover:bg-red-600 transition text-white font-medium px-4 py-2 rounded-md text-sm"
          href="/logout"
        >
          Logout
        </a>
      </div>

      <!-- Edit Post Section -->
      <h2 class="text-xl font-semibold mt-4">Edit Your Post</h2>
      <p class="text-zinc-400 text-sm mb-5">Modify your post content below.</p>

      <form method="post" action="/update/<%= post._id %>" class="mt-3">
        <textarea
          name="content"
          class="block w-full p-3 text-white outline-none resize-none bg-zinc-900 border-2 border-zinc-700 rounded-md focus:border-yellow-500 transition h-32"
        ><%= post.content %></textarea>
        
        <button
          type="submit"
          class="mt-4 w-full bg-yellow-500 hover:bg-yellow-600 transition text-black font-medium px-5 py-2 rounded-md text-sm"
        >
          Update Post
        </button>
      </form>
    </div>

  </body>
</html>
